import SwiftUI

@main
struct GitSwitcherApp: App {
    var body: some Scene {
        MenuBarExtra("GitSwitcher", systemImage: "person.crop.circle") {
            Text("Loading...")
        }
    }
}
